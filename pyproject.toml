[project]
name = "stung"
version = "0.0.1"
description = ""
authors = [
    { name = "HJ Ji", email = "hji20@jh.edu" },
]
license = { text = "MIT" }
readme = ""
requires-python = ">=3.13"
dependencies = [
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["stung"]

[tool.setuptools.package-dir]
"" = "src"